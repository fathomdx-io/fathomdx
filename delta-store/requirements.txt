# Delta-store runtime dependencies. Moved out of the Dockerfile so
# pip-audit, dependabot, and anything else that scans requirements
# files can actually see the dep set. Upper bounds cap the major
# version to keep rebuilds reproducible.
fastapi>=0.115,<1.0
uvicorn>=0.34,<1.0
pydantic>=2.10,<3.0
asyncpg>=0.29,<1.0
pgvector>=0.3,<1.0
open-clip-torch>=2.24,<3.0
# Pillow >=12.2.0 to pick up fixes for CVE-2026-25990 and CVE-2026-40192.
Pillow>=12.2.0,<13.0
python-multipart>=0.0.20,<0.1
httpx>=0.28,<1.0
numpy>=1.26,<3.0
